# v0.4.0 検証

実行日: 2026-09-12 UTC。ローカルNode 26.1.0、tmpfs上の試験DB。

- `npm run check`: 136 tests、構造の3条件比較、15 Docs例の型検査・実行、7つの出力照合、VitePress build。
- `npm run format:check`、`git diff --check`。
- `npm run example:history`: 2期間、4モデル呼出し、10編集、既存topic再利用（決定的モデル）。
- `ATOM_V03_PACKAGE=<published 0.3.0 package> node scripts/check-v03-migration.mjs`: npmの0.3.0 tarballからSQLiteを作成し、0.4へ再接続。3ベクトルを再埋め込み0件で移行、差分進捗・派生物の根拠を保持。
- 同じ本文・固定ベクトル・予算で正しい構造／構造なし／同数の誤った関係を比較。関連条件の取得は1／0／0。これは構造の寄与を切り分ける小さなfixtureで、一般的な意味検索品質ではない。

削除した旧selector・旧ハーネスの試験は、新しいAPIの回帰試験数に合算しない。保存の契約（多重所属、改訂、CAS、原子性、出典、権限、物理purge）は `test/acceptance.test.mjs` とクライアント試験で維持する。`test/fixtures.mjs` は指定された実Storageを返すよう修正し、SQLiteケースがメモリadapterへ誤って検査しないことも確認した。

新しい実LLM評価は未実施。Sakanaに質問を渡す前の2期間の合成履歴をLing Writerで整理する隔離試験を用意したが、OpenRouterキーがないため今回の成功結果には含めない。過去のQwen・DeepSeek測定を0.4の品質結果として流用しない。

npm consumer・GitHub CI・Docs deploymentの最終結果は `release.json` に記録する。
