# Atom Memory 0.9.0 validation

2026-09-14. This record covers the declarative Writer API and one-hop inspect change.

- `npm run check`: 246 core tests, 16 research tests, ranking/selection fixtures, 14 typechecked runnable documentation examples and 6 documented outputs; docs build passed.
- Minimum Node 22.13.0: all 246 core tests passed.
- Empty consumer from the local npm tarball: 91 tests, strict TypeScript, three shipped examples and actual 0.8 package migration passed.
- Actual prior package migrations: 0.4.0, 0.5.1, 0.6.0 and 0.7.0 passed on temporary data. 0.8.0 migration passed on Memory and SQLite, preserving revisions/use, invalidating old cursors, exercising local cycles, reconnect replay and generation invalidation.
- `npm run format:check`, `git diff --check`, and Wrangler docs dry-run passed.
- Sakana `npm run check` passed against this library, including Writer continuation, scoped public projections, permission races, generation fences and exact refresh acknowledgement.

The package content/integrity record is `v0.9.0.json`. Publication and deployed docs are recorded separately in `docs/public/release.json` after readback.

These are deterministic contract checks. No new real Ling run, semantic-quality claim, full Evex-history run, provider cost measurement or production Bot deployment is included. The compositional numerical evaluator remains research, not the default retrieval path.
